import { ConponentLoaderDirective } from './conponent-loader.directive';

describe('ConponentLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ConponentLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
